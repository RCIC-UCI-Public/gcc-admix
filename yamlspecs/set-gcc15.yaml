!include packages.yaml
---
versions: versions-gcc15.yaml
system:
   - elfutils-debuginfod
   - iproute
build_set_specific:
   - annobin-gcc15
bootstrap:
   - "{{bootstrap0}}"
   - "{{bootstrap1}}"
build:
   - "{{build0}}"
manifest:
   - "{{bootstrap1}}"
   - "{{build}}"

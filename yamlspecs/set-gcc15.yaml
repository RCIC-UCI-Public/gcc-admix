!include packages.yaml
---
versions: versions-gcc15.yaml
build_set_specific:
   - annobin
bootstrap:
   - "{{bootstrap0}}"
   - "{{bootstrap1}}"
build:
   - "{{build0}}"
manifest:
   - "{{bootstrap1}}"
   - "{{build}}"
